/* 5 Create an arrow function called square that takes a number as an argument and returns its square Use
the arrow function to calculate the square of a given number and display the result.
   */

const square = (num) => num*num;
const number = 10;
const squaredNumber = square(number);
console.log("Square:", squaredNumber);

//output  Square: 100